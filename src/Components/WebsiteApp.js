import React from "react";
import "../CSS/WebsiteApp.css";
const WebsiteApp = (props) => {
    return(
        <div className="websiteApp">
            <p>Hello! I'm Calin!</p>
            <p>This React web application is under development.</p>
            <p>I am currently changing the old website into a modern React App.</p>
        </div>
    );
}

export default WebsiteApp;